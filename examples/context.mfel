let o = context () {
    let x = 1;
    let y = context () {  
        let z = 2;
        lambda(p){let old = z; z = p; old}
    };
    lambda(p){x+y.z+p}
};

@println(o.y.z);
@println(o(100));
@println(o.y(5));
@println(o.y(20));
@println(o.y.z);

