defun f x {
    if x == nil {
        nil
    } 
    else {
        if x > 5 {
            nil
        } else {
            x+1
        }
    }
}

@println(f(f(f(f(f(f(1)))))));
@println(1 | f | f | f | f | f | f | f);

@println(f(f(f(1))));
@println(1 | f | f | f);
