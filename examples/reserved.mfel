let returning = 10;
let breaker = 20;

let ing = 1000;
let er = 2000;

let bindings = [["x",1],["y",2]];

@println(bindings);

let ings = lambda(x) {
    if x < 0 {
        0
    } else {
        ings(x-1)
    }
};
bind ings;

@println(returning);
@println(breaker);

#return ing;
break er;