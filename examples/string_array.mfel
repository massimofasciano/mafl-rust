### loads a file and replaces = with :
### shows conversion from string to array of char and back to string

let str = @readfile("Cargo.toml");
let chars = @array(str);
let chars2 = @std.iter.map(
    \x { if x == '=' { ':' } else { x } },
    chars
);
let str2 = @string(chars2);
@println(str2);
