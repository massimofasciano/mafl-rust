### the @test builtin takes 2 arguments:
### 1- an expression as a string
### 2- an expected result as an expression
###
### it evals the string expression and checks if it matches 
### the expected result, printing the results...
###
### this is useful for unit testing
###

let x = 20;
let f = fun x y { x / y };

@test("1 + 5 * 8 / x + f(3,4)",3.75);
@test("1 + 1",3);
@test("try y catch e {@is_error(e)}",true);
