let z := 1000;
let test := module (z) {
    let x := 10;
    let y := 20;
    let f := closure() { x + y };
    let m := module (x,y,z) {
        # shadows x and y (by value)
        let f := closure() { x + y + z };
        let c2 := @context;
    };
    let c1 := @context;
};


@println(test.m.f());
