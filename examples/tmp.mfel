let a = 10;
let c = 100;
let f = fun x y with a b {
    x * a + y * b + c
};
f.b = 20;
c += 1;
@println(f(3,4));
c += 1;
@println(f(3,4));
(#
let f = fun x as me {
    if x < 2 { x }
    else {
        me(x-1)* 2        
    }
};
#)
defun f x {
    if x < 2 { x }
    else {
        f(x-1)* 2        
    }
}
#(\x y -> x+y)(3,4)
#\x y { x+y }

let x = 10;
let f = fun with x { x = x + 1 };
@println(x);
f();
@println(x);

let sum = fun iter {
    fold(fun x y {x + y}, 0, iter)
};

sum