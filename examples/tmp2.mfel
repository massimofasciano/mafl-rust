#(1.0 * "allo")?

function f(x) {
    try 1/x catch e {
        if @string(e) == "division by 0" {
            @println("you divided by zero");
            0
        } else {
            -1
        }
    }
}

f(0)