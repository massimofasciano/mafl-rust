let x := 1;
let o := capture (x) {
    let f := closure(y) {x+y};
    let z := 3;
};
#o.f(5+o.z)
@println(@get(o,"x"));
@println(@get(o,"f"));
@println(@get(o,"z"));
@println(@get(o,"f")(10));

@set(o,"z",@get(o,"z")+1000);
@println(@get(o,"z"));
@insert(o,"w",13);
@println(o.w);
