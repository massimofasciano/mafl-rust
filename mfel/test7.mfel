# fix infix function op

#var add = lambda(x,y) { x+y };
#add(1,2) $add 3
#1 $add 2
#2 $@pow 3
#2 ^ 3

#var x = 100;
#let x = 10 in x + 1; x

(#
var x = let i = 0 in let j = 1 in while i < 10 {
    @print(i);
    i := i + 1;
    j := j * 2
};
x
#)

@eval("@println(\"hello\")");
var add = lambda(x,y) { x+y };
var f = lambda (x) {
    if x = () {
        ()
    } 
    else {
        if x > 5 {
            ()
        } else {
            x+1
        }
    }
};
#f(f(f(f(f(f(f(1)))))))
#1 | f | f | f | f | f | f | f
1 | f | add(2) | f
